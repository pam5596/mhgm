// tsconfig.vitest.json (新規作成、プロジェクトルート)
{
  "compilerOptions": {
    "composite": true,
    "types": ["vitest/globals", "node"]
  },
  "include": [
    "shared/**/*",
    "test/**/*",
    "vitest-auto-imports.d.ts" // 前回生成したファイルもここでカバーする
  ]
}